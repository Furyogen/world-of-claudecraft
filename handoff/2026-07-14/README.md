# WOC Map Editor — Handoff (Tues, 14 Jul 2026)

Bundle prepared for **@EnriqueGF**, **@MasterZensei**, **@maxpolaczuk**.

## What's here
- **`EDITOR_NOTES.md`** — full functionality notes + how to run the editor
  (locked to port **5173**, open `/editor.html`).
- **`scorching_wastes.ts`** — the latest Scorching Wastes desert-map zone content
  included in the build (level 20–30 endgame desert zone; boss: Karesh the
  Parched). Lives in the repo at `src/sim/content/scorching_wastes.ts`.

## The repo
The full runnable editor is a **private** GitHub repo:

> `github.com/troypolaczuk/woc-map-editor`

You've been invited as a collaborator. Clone it, `npm install`, `npm run dev`,
then open **http://localhost:5173/editor.html**. See `EDITOR_NOTES.md` for the
full tour.

*This same folder is committed into the repo under `handoff/2026-07-14/`.*
